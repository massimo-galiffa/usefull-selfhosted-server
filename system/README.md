# Docker Compose Autostart with systemd

To automatically start your homeserver services after a reboot, you can use a systemd service.

##  Create the service file

Place the file here:

/etc/systemd/system/