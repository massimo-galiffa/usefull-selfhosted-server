version: "3.9"

services:
  minecraft:
    image: itzg/minecraft-server
    container_name: minecraft
    restart: always
    ports:
      - "25565:25565"
    environment:
      EULA: "TRUE"
      MEMORY: ${MC_RAM}
    volumes:
      - minecraft_data:/data

volumes:
  minecraft_data: